public abstractclass Expense
{
    id, amount, padidby, Lit<Splits>

    validate()
}

Split(user, amount)

EqualSplit, ExactSplit(amount), PercentSplit(percent)

EqualEpense, ExactExpense, PErcentExpese

ExpenseService
createExpense(expneseType, amount, paidsBy, Lsit<SPlits>, metafdat) //   set split amount;update reposirty here
showBalance()
showBalances()

Use(id, name, email)

ExpenseReposiroty Map<String, Map<Strong Double>>
UserReposirty Map<Stirn, uSer?


Main(

    ExpenseService
    addUsers
    createExpense
  
)




